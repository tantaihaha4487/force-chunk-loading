{
  "schemaVersion": 1,
  "id": "force_chunk_loading",
  "version": "${version}",

  "name": "Force Chunk Loading",
  "description": "A server-only, craftable Earth-textured player head that keeps its chunk loaded.",
  "authors": ["Thanachot"],
  "contact": {},

  "license": "MIT",

  "environment": "server",
  "mixins": [
    "force_chunk_loading.mixins.json"
  ],
  "entrypoints": {
    "main": [
      "com.tantaihaha.forcechunkloading.force_chunk_loading.ForceChunkLoading"
    ]
  },
  "depends": {
    "fabricloader": ">=${loader_version}",
    "java": ">=25",
    "fabric-api": "*",

    "minecraft": "~${minecraft_version}"
  }
}
